package com.android.asilvia.softcoin.ui.base.navigation;


/**
 * This class will contain all the navigation in the app
 * Created by asilvia on 26-10-2017.
 */

public class AppNavigation {
/*

    public static void goToMain(Activity activity) {
        Intent intent = new Intent(activity, MainActivity.class);
        activity.startActivity(intent);
        activity.finish();
    }


    public static void goToWelcome(Activity activity) {
        Intent intent = new Intent(activity, WelcomeActivity.class);
        activity.startActivity(intent);
        activity.finish();
    }


    public static void goToPairing(Activity activity) {
        Intent intent = new Intent(activity, StartActivity.class);
        intent.putExtra(AppConstants.NEXT_SCREEN, AppConstants.PAIRING_SCREEN);
        activity.startActivity(intent);
        activity.finish();
    }

    public static void goToPairingFromFragment(StartActivity activity)
    {
        final FragmentTransaction fragmentTransation = activity.getSupportFragmentManager().beginTransaction();
        fragmentTransation.replace(R.id.fragment_container, new PairingFragment(), PairingFragment.TAG);
        fragmentTransation.commit();
    }

    public static void goToPorgotPasswordFromFragment(StartActivity activity)
    {
        final FragmentTransaction fragmentTransation = activity.getSupportFragmentManager().beginTransaction();
        fragmentTransation.replace(R.id.fragment_container, new ForgotPasswordFragment(), ForgotPasswordFragment.TAG);
        fragmentTransation.commit();
    }


    public static void goToCheckEmailFromFragment(StartActivity activity)
    {
        final FragmentTransaction fragmentTransation = activity.getSupportFragmentManager().beginTransaction();
        fragmentTransation.replace(R.id.fragment_container, new CheckEmailFragment(), CheckEmailFragment.TAG);
        fragmentTransation.commit();
    }

    public static void goToLogInFromFragment(StartActivity activity)
    {
        final FragmentTransaction fragmentTransation = activity.getSupportFragmentManager().beginTransaction();
        fragmentTransation.replace(R.id.fragment_container, new LoginFragment(), LoginFragment.TAG);
        fragmentTransation.commit();
    }

*/

}
